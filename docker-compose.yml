name: ecommerce-news-agent

services:
  n8n:
    image: docker.n8n.io/n8nio/n8n
    container_name: ecommerce-news-agent-n8n
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${N8N_PORT:-5678}:5678"
    volumes:
      - n8n_data:/home/node/.n8n

volumes:
  n8n_data:
